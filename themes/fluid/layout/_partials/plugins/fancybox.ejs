<%
import_css(theme.static_prefix.fancybox.url, 'jquery.fancybox.min.css')

import_script(`
<script>
  Fluid.utils.createScript('${url_join(theme.static_prefix.fancybox.url, 'jquery.fancybox.min.js')}', function() {
    Fluid.plugins.fancyBox();
  }, '${theme.static_prefix.fancybox.integrity}');
</script>
`)
%>
