---
import PostCard from "@/components/PostCard.astro";
import { _t, SITE } from "@/config";
import BaseLayout from "@/layouts/BaseLayout.astro";
import getBlogPosts from "@/utils/getPosts";

const posts = await getBlogPosts();
const featuredPosts = posts.filter((post) => !!post.data.featured);
const recentPosts = posts.filter((post) => !post.data.featured).slice(0, SITE.postsPerHomepage);
---

<BaseLayout title={SITE.title} description={SITE.description}>
  <main id="main-content">
    <section>
      <h1 class="sr-only">{SITE.title}</h1>
      {
        featuredPosts.length > 0 && (
          <>
            <h2>{_t.common.featuredPost}</h2>
            <ul>
              {featuredPosts.map((post) => (
                <li>
                  <PostCard
                    id={post.id}
                    title={post.data.title}
                    description={post.data.description}
                    date={post.data.date}
                    updated={post.data.updated}
                  />
                </li>
              ))}
            </ul>
            <hr />
          </>
        )
      }
      <h2>{_t.common.recentPost}</h2>
      <ul class="post-list">
        {
          recentPosts.map((post) => (
            <li>
              <PostCard
                id={post.id}
                title={post.data.title}
                description={post.data.description}
                date={post.data.date}
                updated={post.data.updated}
              />
            </li>
          ))
        }
      </ul>
      <div class="view-all">
        <a href="/posts">{_t.common.viewAllPosts}</a>
      </div>
    </section>
  </main>
</BaseLayout>

<style>
  h2 {
    margin-top: 0;
    margin-bottom: 3rem;
    font-size: var(--font-size-small);
    font-weight: var(--font-weight-light);
    line-height: 1.2;
    color: var(--color-700);
  }
  ul {
    list-style-type: none;
    margin: 0;
    padding: 0;
  }
  hr {
    border: none;
    border-top: 1px solid var(--color-500);
    margin: 2rem 0 3rem 0;
  }
  .view-all {
    margin: 3rem 0 1rem 0;
    text-align: center;
  }
  .view-all a {
    text-decoration: none;
  }
  .post-list {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }
</style>
