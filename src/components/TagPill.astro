---
interface Props {
  id: string;
  name: string;
  count?: number;
  active?: boolean;
}

const { id, name, count, active } = Astro.props;
---

<a href={active ? "/posts" : `/tags/${id}`} data-astro-prefetch>
  <div class={`tag-pill ${active ? "active" : ""}`}>
    <span>{name}</span>
    {count !== undefined && <span class="count">{count}</span>}
  </div>
</a>

<style>
  a {
    text-decoration: none;
  }
  .tag-pill {
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    padding: 0.2rem 0.6rem;
    border-radius: 4px;
    border: 1px solid var(--color-500);
    font-size: var(--font-size-xs);
    letter-spacing: 0.01em;
    color: var(--color-700);
  }
  .tag-pill.active,
  a:hover .tag-pill {
    color: var(--color-900);
    border-color: var(--color-900);
  }
  .count {
    font-family: var(--font-mono);
    font-size: 0.8em;
    opacity: 0.6;
  }
</style>
